node_modules
.pnpm-store
.next
dist
coverage
*.tsbuildinfo
.DS_Store
.claude/

# Local Netlify folder
.netlify

# Environment files — never commit (only .env.example is tracked)
.env
.env.*
!.env.example

# Secrets — never commit certificates
certs/
*.p12
*.pem
*.key
