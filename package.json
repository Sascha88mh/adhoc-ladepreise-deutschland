{
  "name": "adhoc-plattform",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.31.0",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r --if-present lint",
    "test": "pnpm -r --if-present test",
    "ingest:sync": "pnpm --filter ingest sync"
  },
  "dependencies": {
    "axios": "^1.15.0",
    "pg": "^8.20.0",
    "zod": "^4.3.6"
  }
}
