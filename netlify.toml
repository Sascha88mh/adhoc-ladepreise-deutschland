[build]
  base = "apps/web"
  command = "cd ../.. && pnpm install --frozen-lockfile && pnpm --filter web build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "10.31.0"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# Mobilithek-Push wird zuerst von netlify/edge-functions/mobilithek-webhook.ts
# verarbeitet. Diese Redirects bleiben als Fallback fuer unkomprimierte Payloads
# bestehen, falls die Edge Function einmal nicht greift.
[[redirects]]
  from = "/api/admin/mobilithek/webhook/:feedId"
  to = "/.netlify/functions/mobilithek-webhook/:feedId"
  status = 200
  force = true

[[redirects]]
  from = "/api/mobilithek/webhook"
  to = "/.netlify/functions/mobilithek-webhook"
  status = 200
  force = true

[[headers]]
  for = "/api/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    X-Frame-Options = "DENY"
